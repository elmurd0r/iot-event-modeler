version: "3.8"

services:
  mysql:
    image: mysql:8.0.26
    container_name: mysql-db
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - C:\Users\Yusuf\Desktop\mysql:/var/lib/mysql
# docker-compose -f filename up -d
